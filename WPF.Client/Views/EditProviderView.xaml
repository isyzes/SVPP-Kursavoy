<Window x:Class="WPF.Client.Views.EditProviderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WPF.Client.Views"
        Title="Поставщик" 
        Height="300" 
        Width="600"
        WindowStartupLocation="CenterOwner"
       >

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

  
        <!-- Основные поля формы -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Название компании -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Название компании*:"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         Text="{Binding CarentProvider.Name, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <!-- Номер телефона -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Телефон*:"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         Text="{Binding CarentProvider.Phone, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <!-- Адрес -->
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Адрес:"/>
                <TextBox Grid.Row="5" Grid.Column="1" 
                         Text="{Binding CarentProvider.Address, RelativeSource={RelativeSource AncestorType=Window}}"
                         Height="60"
                         TextWrapping="Wrap"
                         VerticalContentAlignment="Top"
                         AcceptsReturn="True"/>

            </Grid>
        </ScrollViewer>

        <!-- Кнопки действий -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сохранить" 
                        Command="{Binding SaveProviderCommand}"
                        FontWeight="Bold"/>

                <Button Content="Отмена" 
                        IsCancel="True" />
            </StackPanel>
        </Border>
    </Grid>
</Window>